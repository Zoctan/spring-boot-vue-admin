<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zoctan.api.mapper.UserMapper">

    <resultMap id="UserMap" type="com.zoctan.api.model.User">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="avatar" jdbcType="VARCHAR" property="avatar"/>
        <result column="resume" jdbcType="VARCHAR" property="resume"/>
        <result column="register_time" jdbcType="TIMESTAMP" property="registerTime"/>
        <result column="last_login_time" jdbcType="TIMESTAMP" property="lastLoginTime"/>
    </resultMap>

    <resultMap id="UserWithRoleMap" type="com.zoctan.api.model.User" extends="UserMap">
        <result column="roleName" jdbcType="VARCHAR" property="roleName" />
    </resultMap>

    <select id="findAllUserWithRole" resultMap="UserWithRoleMap">
        SELECT
        u.*,
        r.name as roleName
        FROM `user` u
        LEFT JOIN `user_role` ur ON u.id = ur.user_id
        LEFT JOIN `role` r ON ur.role_id = r.id
    </select>

    <resultMap id="UserDetailMap" type="com.zoctan.api.model.User" extends="UserWithRoleMap">
        <!-- 1 role -> ..auth -->
        <collection property="authList" ofType="String">
            <result column="auth" jdbcType="VARCHAR" property="code" />
        </collection>
    </resultMap>

    <select id="findDetailBy" resultMap="UserDetailMap">
        SELECT
        u.*,
        r.name as roleName,
        a.code as auth
        FROM `user` u
        LEFT JOIN `user_role` ur ON u.id = ur.user_id
        LEFT JOIN `role` r ON ur.role_id = r.id
        LEFT JOIN `role_auth` ra ON r.id = ra.role_id
        LEFT JOIN `auth` a ON ra.auth_id = a.id
        <where>
            <if test="id != null">u.id = #{id}</if>
            <if test="email != null">u.email = #{email}</if>
            <if test="username != null">u.username = #{username}</if>
        </where>
    </select>

</mapper>